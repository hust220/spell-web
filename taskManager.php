<?php

if(!isset($_GET[ 'task'] )) {
	$jsonparams[ 'error'] = "Task parameters not set";
	echo getJSONString($jsonparams);
	die;
}
$task = $_GET[ 'taskid'];

if(!isset($_GET[ 'filetype'])) {
	$jsonparams[ 'error'] = "File type unknown";
	echo getJSONString($jsonparams);
	die;
}

switch($taskID) {
	case: "
}

?>
